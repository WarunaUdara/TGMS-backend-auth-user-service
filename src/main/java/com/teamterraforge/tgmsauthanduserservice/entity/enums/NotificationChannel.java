package com.teamterraforge.tgmsauthanduserservice.entity.enums;

/**
 * Communication channels for notifications
 * Corresponds to notification_channel enum in PostgreSQL
 */
public enum NotificationChannel {
    EMAIL,
    SMS,
    PUSH
}
