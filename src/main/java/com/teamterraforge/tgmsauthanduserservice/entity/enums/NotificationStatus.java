package com.teamterraforge.tgmsauthanduserservice.entity.enums;

/**
 * Delivery status for notifications
 * Corresponds to notification_status enum in PostgreSQL
 */
public enum NotificationStatus {
    PENDING,
    SENT,
    FAILED
}
